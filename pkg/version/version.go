package version

var (
	// Product Name of the package
	Product = "UNKNOWN"
	// Repository URL of the repository
	Repository = "UNKNOWN"
	// Version Version
	Version = "UNKNOWN"
	// Timestamp Timestamp of build generation
	Timestamp = "UNKNOWN"
)
